doctype html
html(lang='en')
  include common/head.pug
  +head("Starter MVP")
  body
    header
      include common/navbar.pug
      +navbar(user)
      .container
        h1= message
        h2= error.status
        pre #{error.stack}
